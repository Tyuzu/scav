// MediaRenders - Consolidated component
// Re-exports from shared for backward compatibility
export { RenderVideoPost } from "../../../shared/components/MediaRenders.js";
        muted: true
      },
      v
    );

    container.appendChild(video);
  });
}

export { RenderVideoPost };
